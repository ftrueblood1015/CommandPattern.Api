@page "/mtgset"
@page "/mtgset/{EntityId:int}"

@using CommandPattern.BlazorServer.Shared.Components;
@using CommandPattern.BlazorServer.Shared;
@using CommandPattern.Domain.Entities.Mtg;

@inherits DetailPageBase<Set>;

<MudText @attributes='Splats.PageHeader("MtgSetDetail")'>MtgSetDetail</MudText>

<MudForm @ref="Form" @bind-IsValid="@success">
    <MudPaper Class="pa-4">
        <EntityBaseFrom Entity="@Entity"></EntityBaseFrom>
        <MudGrid>
            <MudItem xs="12" md="12" lg="12">
                <MudNumericField @bind-Value="@Entity!.ReleaseYear" @attributes='Splats.NumericRequired("ReleaseYear")' />
            </MudItem>
        </MudGrid>
    </MudPaper>
    <MudPaper Class="pa-4">
        <MudButton @attributes="Splats.NewButton()" OnClick="@(() => Save(Entity!, "mtgsets"))">Save</MudButton>
        <MudButton @attributes="Splats.ResetButton()" OnClick="@(()=>Form!.ResetAsync())" Class="mx-2">Reset</MudButton>
        <MudButton @attributes="Splats.CancelButton()" OnClick="@(() => Cancel("mtgsets"))">Cancel</MudButton>
    </MudPaper>
</MudForm>